# Legacy Prototype Reference

This folder contains the original CDN/local-file prototype and uploaded source-system examples.

Use it as reference only:

- Estimating Master V4 workbook examples
- FS Estimator V2 behavior reference
- FS Job Dashboard behavior reference
- Earlier UI and workflow ideas

Do not add production app behavior here. The production app lives in `src/`, and Supabase persistence belongs in the repository helpers under `src/lib/`.
